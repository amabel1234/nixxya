{
  "version": 2,
  "installCommand": "npm install -g pnpm && NODE_ENV=development pnpm install --no-frozen-lockfile",
  "buildCommand": "NODE_ENV=production pnpm --filter @workspace/nixx-ai run build && node api/build-vercel.mjs",
  "outputDirectory": "artifacts/nixx-ai/dist",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ],
  "env": {
    "VERCEL": "1"
  }
}
